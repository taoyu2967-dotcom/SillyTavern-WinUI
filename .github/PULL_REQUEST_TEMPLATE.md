## What does this PR change?

<!-- One or two sentences. -->

## Checklist

- [ ] `dotnet build -p:Platform=x64` passes with 0 warnings
- [ ] No changes to SillyTavern's own `public/` frontend files
- [ ] No shell string interpolation for process launches (fixed `node` + `ArgumentList`)
- [ ] No fake driver/GPU-level toggles
- [ ] README (EN / 简体中文) updated if user-facing behavior changed
